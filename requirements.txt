# The mcp 2.x API (MCPServer, snake_case model fields) is not compatible with
# 1.x, which used decorators on a low-level Server, so this pin is a real
# constraint rather than caution.
mcp>=2.0,<3

# HTTP client for the cam2ip MJPEG stream. httpx2 rather than httpx because mcp
# already depends on it -- one HTTP stack in the image instead of two.
httpx2>=2.5.0

# starlette and uvicorn arrive via mcp and are not imported directly.
